import { Routes } from '@angular/router';
import { PackageTrackingComponent } from './package-tracking/package-tracking.component';

export const routes: Routes = [
    { path: '', component: PackageTrackingComponent}
];
